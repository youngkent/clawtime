# ─── ClawTime Configuration ───

# Server port
PORT=3000

# ─── OpenClaw Gateway ───
# WebSocket URL for the OpenClaw gateway
GATEWAY_URL=ws://127.0.0.1:18789

# Gateway authentication token
GATEWAY_TOKEN=your-gateway-token-here

# Session key to use when sending messages (OpenClaw session identifier)
SESSION_KEY=agent:main:main

# ─── Bot Identity ───
# Display name shown in the chat UI
BOT_NAME=ClawTime

# Emoji shown as the bot's avatar
BOT_EMOJI=🤖

# Tagline shown on the welcome screen
BOT_TAGLINE=Your AI assistant. Type a message to start chatting.

# ─── Features ───
# Enable the 3D avatar panel (true/false)
ENABLE_AVATAR=true

# Enable the /api/inject endpoint for pushing messages from localhost (true/false)
ENABLE_INJECT=true

# ─── Theme ───
# Primary accent color (CSS hex without #)
THEME_ACCENT=f97316

# ─── Session ───
# Session TTL in hours (how long a passkey login lasts)
SESSION_TTL_HOURS=24

# ─── Voice / TTS (optional) ───
# Enable voice mode button (browser SpeechRecognition + server-side TTS)
# ENABLE_VOICE=true

# Path to edge-tts binary (used for server-side TTS in voice mode)
# EDGE_TTS_BIN=edge-tts

# TTS voice name (see: edge-tts --list-voices)
# TTS_VOICE=en-US-ChristopherNeural

# ─── STT (optional) ───
# Enable server-side speech-to-text (requires whisper binary)
# ENABLE_STT=false
# WHISPER_BIN=/usr/local/bin/whisper-transcribe

# ─── Task Panel (optional) ───
# Enable task list panel (read/write a markdown file)
# ENABLE_TASKS=false
# TASKS_FILE=/path/to/TODO.md
